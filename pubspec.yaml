name: payments_challenge_yuca
description: Yuca challenge

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  cupertino_icons: ^1.0.2
  flutter:
    sdk: flutter
  http:
  mobx:
  page_transition:
  flutter_mobx:
  get_it:

dev_dependencies:
  flutter_lints: ^1.0.0
  mobx_codegen:
  build_runner:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
